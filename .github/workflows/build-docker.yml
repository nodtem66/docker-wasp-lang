name: Build images

on: [release]

env:
    USERNAME: nodtem66

jobs:
    build-and-push:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3